# Multi-TEE support patch for a34x

Support multiple firmwares (so multiple variants) by shipping different TEE blobs which are dynamically mounted at boot time.

Only latest firmware for each model is supported.
